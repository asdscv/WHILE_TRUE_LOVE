# 변경 기록

## 1.1.7 — 2026-08-25

**음악 버튼이 신랑신부 그림으로 바뀌었습니다.** 음악이 켜져 있으면 두 사람이
춤을 추고, 끄면 멈춰 섭니다. 전에는 `♪` / `♪̸` 글자였습니다.

움직임 자체가 상태 표시입니다 — 켜짐·꺼짐을 색이나 사선으로 알리는 대신,
흔들리는지 아닌지로 읽습니다.

**버튼을 40px → 56px 로 키웠습니다.** 전신 그림이라 40px 안에서는 형체가 잡히지
않습니다. 서리 낀 원판은 남겼습니다. 이 버튼은 인트로 영상(어두움) → 표지 사진 →
본문 종이색을 차례로 지나가는 자리라, 판이 없으면 흰 드레스가 종이색에 묻힙니다.

넣으면서 손본 것:

- 두 장을 같은 칸에 겹쳐 두고 **투명도로만** 갈아 끼웁니다. `src` 를 갈아끼우면
  처음 켤 때 움직이는 판을 그제서야 내려받느라 버튼이 한 번 빕니다.
- 원본 GIF 대신 **애니메이션 webp** 로 넣었습니다(16프레임, 70ms, 1.12초 한 바퀴).
  GIF 는 투명도가 1비트뿐이라 부드러운 테두리가 계단처럼 깨지는데, 원판 위에
  얹히는 그림이라 그 자국이 그대로 보입니다. 141KB → 90KB 로 줄기도 했습니다.
- 두 장 모두 그림 둘레의 빈 여백을 잘라내고 높이를 160px 로 맞췄습니다. 원본은
  여백 크기가 서로 달라서, 그대로 쓰면 켜고 끌 때마다 사람이 커졌다 작아집니다.
  껐을 때 그림은 1254px PNG(1.1MB) → 112×160 webp(10KB) 로 줄였습니다.
- 쓰임이 사라진 `@keyframes spin` 을 지웠습니다(옛 `♪` 를 돌리던 것).

## 1.1.6 — 2026-08-25

**배경음악을 "Beautiful Love" 로 바꿨습니다.** 전에 깔려 있던
"Coffee and Moonlight" 는 함께 지웠습니다.

`public/bgm/beautiful-love.mp3` — 3분 32초, 192kbps 스테레오(44.1kHz), 4.8MB.
기존 곡과 같은 규격이라 다시 인코딩하지 않고 그대로 넣었습니다. 곡이 52초 길어진
만큼 파일도 3.7MB → 4.8MB 로 커졌습니다.

파일명은 새로 지었습니다(`Beautiful Love.mp3` → `beautiful-love.mp3`). 공백이 든
이름은 주소로 만들 때 `%20` 으로 새고, 폴더의 기존 표기와도 어긋납니다. 겸사겸사
파일명이 달라져서 예전 곡을 받아 둔 브라우저도 캐시를 붙들지 않고 새로 받습니다.

재생 방식은 그대로입니다 — 진입 시 자동 재생을 시도하고, 브라우저가 막으면 첫
터치·스크롤에서 시작하며, 우측 상단 ♪ 버튼으로 끄고 켭니다.

## 1.1.5 — 2026-08-25

**계좌 한 줄의 순서를 바꿨습니다.** 관계 → 은행 계좌번호 → 예금주 순으로 읽습니다.
전에는 은행과 관계가 한 줄에 묶여 있고 계좌번호가 그 아래였습니다.

```
아버지
부산은행 0320-1045-6171
예금주 박희서
```

은행 이름은 계좌번호와 같은 줄이되 한 급 작게(15px) 뒀습니다. 둘 다 번호 크기
(19px)로 두면 `국민은행 324701-04-138026` 같은 조합이 360px 폭 폰에서 복사 버튼과
부딪혀 넘칩니다. 가운데 줄은 하객이 옮겨 적는 정보라 번호 쪽을 크게 살렸습니다.

은행·관계를 한 줄에 묶던 `.account-row__top` 은 쓰임이 사라져 없앴습니다.

## 1.1.4 — 2026-08-24

**축의금 계좌 다섯 건을 실제 계좌로 바꿨습니다.** 그동안 들어 있던 값은 모두
자리만 잡아둔 예시였습니다. 예금주 이름은 그대로입니다.

- 신랑측 — 아버지 부산은행 · 어머니 신한은행 · 신랑 토스뱅크
- 신부측 — 어머니 신한은행 · 신부 국민은행

**쌓이는 순서도 어른부터로 바꿨습니다.** 신랑측은 아버지 → 어머니 → 신랑,
신부측은 어머니 → 신부 순입니다(신부 아버님은 고인이시라 행이 없습니다).
`config.account` 의 배열 순서가 그대로 화면 순서라, 배열만 다시 세웠습니다.

계좌는 여전히 **`?account=on` 링크에서만** 보입니다(`account.defaultVisible: false`).
기본 링크로 받은 사람에게는 "마음 전하실 곳" 섹션 자체가 뜨지 않습니다.

## 1.1.3 — 2026-08-24

**사진을 손가락으로 확대할 수 없게 했습니다.** 두 손가락 핀치도, 두 번 탭도
먹지 않습니다. 사진첩 크게보기(라이트박스)는 그대로 열리고, 다만 그 안에서도
더 키울 수는 없습니다.

막는 곳이 세 군데로 나뉜 이유는 브라우저마다 걸리는 지점이 다르기 때문입니다.

- `index.html` 뷰포트 meta 에 `maximum-scale=1.0, user-scalable=no` — 안드로이드
  크롬을 비롯한 대부분이 여기서 걸립니다.
- `html, body` 의 `touch-action: manipulation` — 두 번 탭해서 키우는 동작만
  끄고, 한 손가락 스크롤과 버튼 탭은 그대로 삽니다.
- `src/lib/noZoom.js` — **iOS 사파리는 `user-scalable=no` 를 접근성 이유로 일부러
  무시합니다.** meta 만으로는 아이폰에서 그대로 확대되기 때문에, 웹킷 전용
  `gesturestart`/`gesturechange`/`gestureend` 를 취소하고 손가락이 둘 이상일 때의
  `touchmove` 도 보험으로 막습니다.

데스크톱 브라우저 확대(⌘+ / Ctrl+휠)는 건드리지 않았습니다. 마우스로 보는
사람의 시력 보정까지 뺏을 이유는 없고, 확대가 거슬리는 쪽은 손가락 핀치입니다.

길게 눌러 뜨는 "이미지 저장" 메뉴는 이번 변경과 무관하게 그대로입니다
(첫 화면 영상만 1.1.0 에서 막혀 있습니다).

## 1.1.2 — 2026-08-24

사진첩에 세비야(스페인 광장) 사진 **11장**을 더했습니다. 20장 → **31장**.
찍은 시각(2025-09-17 18:44~19:48) 순으로 뒤에 이어 붙여, 넘길수록 해가 지고
마지막 두 장이 첫 화면 영상과 같은 밤 풍경으로 이어집니다.

가로 사진 3장이 처음으로 섞였습니다. 격자는 원래 정사각으로 잘라 쓰고 크게
볼 때는 `object-fit: contain` 이라, 손볼 곳 없이 그대로 들어갑니다.
긴 변을 1600px 으로 맞추는 방식이라 가로 사진은 1600×1067 입니다.

격자 더보기 칸은 `+23` 이 됩니다. 첫 화면에 내려받는 양은 그대로입니다 —
격자에 깔리는 9장만 먼저 받고 나머지는 크게 볼 때 받습니다.

## 1.1.1 — 2026-08-24

첫 화면을 **탭해서 건너뛰던 것도 막았습니다.** 이제 영상이 끝나야만
(`config.intro.duration` = 5000ms) 넘어갑니다.

탭을 유도하던 아래쪽 화살표도 함께 없앴습니다. 눌러도 아무 일이 일어나지 않는
화살표가 남아 있으면 오히려 답답하게 읽히기 때문입니다.

영상이 못 뜨는 경우에도 타이머는 그대로 돌기 때문에 첫 화면에 갇히지는 않습니다.

## 1.1.0 — 2026-08-24

**전환 전 첫 화면이 사진에서 영상으로 바뀌었습니다.** `src/assets/intro/intro.mp4`
(1080×1620, 4.8초, 소리 없음)가 자동으로 재생됩니다.

영상은 **조작할 수 없습니다.** 재생 컨트롤을 아예 붙이지 않았고, `pointer-events`
까지 꺼서 일시정지·구간이동이 안 되고 길게 눌러도 저장 메뉴가 뜨지 않습니다.
탭은 영상을 통과해 예전처럼 "바로 열기"로만 동작합니다.

머무는 시간은 **영상 길이 + 0.2초 = 5000ms** 로 잡았습니다(`config.intro.duration`).
영상이 끝까지 재생되고 마지막 프레임이 0.2초 머문 뒤 페이드가 시작됩니다.

영상은 `object-fit: contain` 으로 **잘리지 않게** 넣었습니다. `cover` 로 꽉 채우면
세로로 긴 폰에서 좌우가 30% 넘게 잘려 "Love, in the light" 의 뒷부분과 사람 한 명이
통째로 사라집니다. 대신 생기는 위아래 여백은 영상 맨 윗줄(`#838383`)에서 맨 아랫줄
(`#3e3e3e`)로 흐르는 그라데이션으로 채워 경계가 보이지 않게 했습니다.
상단 상태바 색(`themeColor.intro`)도 `#838383` 로 맞췄습니다.

곁들여 넣은 것:
- `poster.webp` — 영상 첫 프레임. 영상이 뜨기 전 잠깐과, 자동재생이 막혔을 때
  (아이폰 저전력 모드) 대신 보입니다. 첫 프레임 그대로라 화면이 튀지 않습니다.
- `splash.jpg`(이전 사진), `intro.gif`(같은 영상의 GIF 판) — 둘 다 보관용으로
  폴더에만 두었고 화면에는 쓰지 않습니다. 빌드에도 들어가지 않습니다.
  GIF 는 mp4 보다 용량이 크고(722KB vs 632KB) 해상도·프레임·색 수가 모두 낮아
  쓸 이유가 없었습니다. 자세한 비교는 `src/assets/intro/README.md` 에 있습니다.

첫 화면에 쓰이지 않게 된 `splash.jpg` 403KB 가 빌드에서 빠지고, 대신 영상 632KB 와
포스터 46KB 가 들어옵니다.

## 1.0.5 — 2026-08-23

사진첩에서 17번째·21번째 사진(바다·노을 컷)을 뺐습니다. 22장 → **20장**.
크게 보기 카운트도 `/ 20` 으로 따라 바뀌고, 격자 더보기 칸은 `+12` 가 됩니다.

`thumbs/` 의 같은 이름 썸네일도 함께 지웠습니다. 남은 파일 이름은 그대로 둬서
`17`·`21` 번호가 비어 있지만(`…16, 18, 19, 20, 22`), 로더가 파일명 순으로만
읽으므로 보이는 순서에는 영향이 없습니다. 되살리려면
`git show 4a98e66:src/assets/gallery/17.webp` 로 꺼내면 됩니다.

## 1.0.4 — 2026-08-23

표지와 약도를 새로 받은 이미지로 바꿨습니다.

**표지**(전환 후 첫 화면)는 같은 시안의 원본 비율 판본입니다. 9:16 으로 잘라
쓰던 것을 2:3 원본 그대로 쓰면서 하늘이 줄고 사진이 커졌습니다. 세로가 짧아진
만큼 첫 화면을 꽉 채우지는 않고 아래에 다음 섹션이 살짝 보입니다. 원본이
5464×8192(42MB)라 폭 1200px · webp q85 로 줄여 넣었습니다(266KB). 프레임 폭이
최대 500px 이라 2.4배면 충분합니다.

**약도**는 손그림 약도(1296×677)에서 선 약도(977×868)로 바뀌었습니다. 이전
그림 안에 들어 있던 "오시는 길" 제목과 이름·주소가 새 그림에는 없습니다.
이름·주소는 원래도 `Location.jsx` 가 텍스트로 따로 보여주고 있어 그대로지만,
**섹션 제목은 이제 폴리오 한 줄(`03 · 오시는 길`)만 남습니다.** 01 초대·02 예식
처럼 큰 제목을 얹을지는 아직 정하지 않았습니다.

원본 PNG 는 `design/` 에 `*-original.png` 으로 두되 `.gitignore` 에 걸었습니다.
표지 원본만 42MB 라 저장소 히스토리에 영구히 얹히는 것을 피했습니다. 다시 뽑는
명령은 `design/README.md` 의 "가공 이력" 에 있습니다.

## 1.0.3 — 2026-08-21

새 맺음말 이미지에도 위쪽 페이드를 넣었습니다. 소나무 가지가 위쪽 프레임에
맞닿아 잘린 자국을 빛에 바래는 것처럼 눕힙니다. 1.0.1 때(여백 70px · 페이드 170px)
보다 조금 넓게 **여백 85px · 페이드 220px** 로 잡았습니다. 오른쪽으로 뻗은 가지
**끝**은 프레임 안쪽에 있어 페이드가 닿지 않습니다.

이미지가 892×1764 에서 892×1849 가 되었습니다. 페이드 정도를 바꾸려면
`design/README.md` 의 뽑는 명령에서 두 숫자(85 / 220)만 함께 조절하면 됩니다.

## 1.0.2 — 2026-08-21

맺음말 이미지를 새로 받은 것으로 교체했습니다(892×1764). 소나무 가지가 **끝까지
프레임 안에 들어와 있어**, 1.0.1 에서 잘린 끝을 가리려고 넣었던 여백·페이드 보정을
걷어냈습니다. 그 보정을 그대로 뒀다면 이번엔 가지 끝이 지워졌을 자리입니다.

지색이 `#f6f3ec` 로 사이트 지색과 이미 같아 색 보정도 필요 없었습니다.
`design/03-footer.webp` 마스터는 받은 PNG 를 무손실 webp 로만 바꾼 것이라
원본과 픽셀이 같습니다(1.8MB → 1.3MB).

## 1.0.1 — 2026-08-21

맨 아래 맺음말 이미지의 소나무 가지가 위쪽에서 **직선으로 뚝 잘려** 보이던 것을
고쳤습니다. 가지 위쪽이 남아 있는 원본은 없어서 가지를 늘려 그릴 수는 없고,
대신 위에 여백을 덧대고 잘린 끝을 빛으로 날려 **역광에 바래 사라지는 것처럼**
이어 보이게 했습니다. 이미지가 899×1750 에서 899×1820 이 되었습니다.

가공은 `design/03-footer.webp` (무손실 마스터, 손대지 않음)에서 뽑습니다.
명령은 `design/README.md` 의 "가공 이력" 에 적어 두었습니다.

## 1.0.0 — 2026-08-21

하객에게 돌려도 되는 첫 완성본. 이 시점의 사이트는
<https://aeshin-jiyong.life> 에 배포되어 있습니다.

### 링크 두 종류
- **소식용**(기본 링크) — 결혼 소식만 전할 분들께. 축의금 계좌와 참석 여부
  (섹션 · 첫 화면 팝업 · 하단바 버튼)가 모두 빠집니다.
- **초대용**(`?account=on`) — 실제로 모실 분들께. 계좌와 참석 여부가 나옵니다.

섹션 번호는 실제로 보이는 섹션만 세어 매기므로 두 링크 모두 번호가 이어집니다.

| | 소식용 | 초대용 |
| --- | --- | --- |
| 폴리오 | 01 초대 · 02 예식 · 03 오시는 길 · 04 축하의 글 · 05 사진 | 01 초대 · 02 예식 · 03 오시는 길 · 04 마음 · 05 참석여부 · 06 축하의 글 · 07 사진 |
| 하단바 | 오시는길 · 축하의 글 | 오시는길 · 참석여부 · 축하의 글 |

### 담긴 것
- 본식 사진 22장. `src/assets/gallery/01~22.webp`(긴 변 1600px)와 같은 이름의
  정사각 썸네일 `thumbs/`(560px). 격자는 썸네일만, 크게 볼 때만 원본 판을 받습니다.
- 사진첩은 3×3 고정. 8장을 깔고 9번째 칸의 `+14` 를 누르면 9번째 사진부터
  전체화면으로 열립니다(격자는 그대로).
- 오시는 길은 접이식이 아니라 늘 펼쳐진 섹션. 손그림 약도 이미지를 쓰고,
  길찾기는 네이버지도 · 카카오맵 · 티맵 버튼으로 넘깁니다.
- 방명록은 Supabase 에 영속화. 참석 여부는 Supabase 기록 + 메일 알림.
- 배경음악, 인트로 전환, D-day, 캘린더 일정 저장, 카카오톡 공유 미리보기.

### 알려진 한계
- 맨 아래 맺음말 이미지의 소나무 가지가 위쪽에서 잘려 있습니다. 원본 PNG 가
  남아 있지 않아(`design/README.md` 참고) 가지를 늘려 그릴 수는 없습니다.
- `src/config.js` 의 축의금 계좌번호는 아직 예시값입니다. 초대용 링크를 돌리기
  전에 실제 계좌로 바꿔야 합니다.
