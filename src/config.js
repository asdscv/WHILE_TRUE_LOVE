// =============================================================
//  청첩장 내용 설정 파일  ⭐️ 실제 정보는 이 파일만 수정하면 됩니다.
// =============================================================
//  - 사진 추가:  src/assets/gallery/ 폴더에 이미지를 넣으면 자동으로 갤러리에 표시됩니다.
//  - 음악 변경:  public/bgm/ 에 mp3 파일을 넣고 아래 bgm.file 값을 파일명으로 바꾸세요.
//  - 계좌 노출:  "계좌 있는 링크 / 없는 링크" 두 버전은 URL 로 구분합니다.
//                · 계좌 없음(기본):  ...github.io/WHILE_TRUE_LOVE/
//                · 계좌 있음:        ...github.io/WHILE_TRUE_LOVE/?account=on
//                기본 노출 여부는 account.defaultVisible 로 조정합니다.
// =============================================================

export const config = {
  // ---------------------------------------------------------
  // 신랑 · 신부
  // ---------------------------------------------------------
  groom: {
    name: '박지용',
    label: '아들', // 아들 / 장남 / 차남 등
    father: '박희서',
    mother: '설옥선',
  },
  bride: {
    name: '오애신',
    label: '딸', // 딸 / 장녀 / 차녀 등
    father: '오현상',
    fatherLate: true, // 고인이신 경우 이름 앞에 '故' 를 붙입니다.
    mother: '김현',
  },
  // 순서 표기(신랑 먼저 / 신부 먼저). true = 신랑 먼저
  groomFirst: true,

  // ---------------------------------------------------------
  // 예식 일시 · 장소
  // ---------------------------------------------------------
  wedding: {
    // D-Day/달력 계산용 (ISO8601, 한국시간 +09:00). 반드시 정확히 입력하세요.
    dateISO: '2026-10-18T12:00:00+09:00',
    // 화면에 보여줄 문구
    dateText: '2026년 10월 18일 일요일',
    timeText: '낮 12시',
    venue: {
      name: '백인제가옥',
      hall: '',
      address: '서울 종로구 북촌로7길 16',
      tel: '', // 대표번호가 있으면 입력하세요. 비우면 전화 버튼이 숨겨집니다.
      // 지도 좌표 (네이버 지도 기준)
      lat: 37.5807993,
      lng: 126.9841756,
      // 네이버 지도 장소 링크 (있으면 '네이버지도' 버튼이 이 링크로 연결됩니다)
      naverUrl:
        'https://map.naver.com/p/entry/place/542868369?lng=126.9841756&lat=37.5807993&placePath=%2Fhome',
    },
  },

  // ---------------------------------------------------------
  // 인사말
  // ---------------------------------------------------------
  greeting: {
    title: '초대합니다',
    // 큰 세리프로 표시되는 리드 문구 (줄바꿈 \n)
    lead: '서로를 바라보다\n이제 같은 곳을\n바라봅니다',
    // 줄바꿈은 그대로 반영됩니다.
    message: `서로가 마주보며 다져온 사랑을
이제 함께 한 곳을 바라보며
걸어갈 수 있는 큰 사랑으로 키우고자 합니다.

저희 두 사람의 새로운 시작을
축복해 주시면 감사하겠습니다.`,
  },

  // ---------------------------------------------------------
  // 인트로 (첫 화면 → 일정 시간 후 본문으로 전환)
  // ---------------------------------------------------------
  intro: {
    enabled: true,
    duration: 3000, // 이 시간(ms) 동안 사진이 머문 뒤 사라지기 시작. 탭하면 즉시.
    fadeMs: 1200, // 사라지는 데 걸리는 시간(ms). CSS 전환 시간과 제거 타이밍이
    //              이 값 하나로 같이 움직인다(둘이 어긋나면 깜빡임이 생김).
    phrase: '저희 두 사람,\n결혼합니다', // 첫 화면 문구 (줄바꿈 \n)
    dateText: '2026. 10. 18.', // 첫 화면 하단 날짜
    // 인트로 배경 사진: src/assets/intro/ 폴더에 이미지를 넣으면 사용됩니다.
    // (없으면 갤러리 첫 사진이 사용됩니다.)
  },

  // 예식일을 하객 캘린더에 저장하도록 안내하는 섹션
  saveTheDate: {
    enabled: true,
    title: '잊지 않도록,\n기억해 주세요',
    message: '소중한 날을 함께해 주실 수 있도록\n캘린더에 일정을 미리 저장해 주세요.',
    durationHours: 2, // 캘린더에 저장될 일정 길이
  },

  // 모바일 브라우저 상단 상태표시줄 영역의 색.
  // 그 영역은 브라우저가 칠하는 곳이라 사진을 그릴 수는 없고 색만 맞출 수 있습니다.
  // 각 이미지의 맨 윗줄 평균색을 넣어두면 이미지가 이어지는 것처럼 보입니다.
  themeColor: {
    intro: '#e7e1db', // 인트로(전환 전) 사진 상단색
    cover: '#aabddc', // 표지 사진 상단색(하늘)
    page: '#f6f3ec', // 표지를 지나 본문을 볼 때(지색)
  },

  // 맨 아래 맺음말 (줄바꿈 그대로 반영)
  footer: {
    message: `평범한 하루하루를 함께 나누고
서로를 아끼며 오래도록 살아가겠습니다.
저희의 시작에 함께해 주셔서 감사합니다.`,
  },

  // ---------------------------------------------------------
  // 배경음악 (페이지 진입 시 재생)
  // ---------------------------------------------------------
  bgm: {
    enabled: true,
    // public/bgm/ 폴더에 넣은 파일명.
    // 파일이 없으면 재생만 안 될 뿐, 페이지는 정상 동작합니다.
    file: 'coffee-and-moonlight.mp3',
    title: 'Coffee and Moonlight', // (선택) 곡 제목 표시용
  },

  // ---------------------------------------------------------
  // 갤러리
  // ---------------------------------------------------------
  gallery: {
    // 실제 사진이 준비되면 src/assets/gallery/ 에 넣으세요(자동 인식).
    // 폴더가 비어 있는 동안 아래 임시 이미지가 사용됩니다.
    // 개수 제한 없음 — 수십 장도 그대로 확장됩니다.
    placeholderCount: 12,
  },

  // ---------------------------------------------------------
  // 오시는 길
  // ---------------------------------------------------------
  location: {
    // 대중교통/자가용/주차 안내 (자유롭게 수정)
    transport: [
      { icon: '🚇', title: '지하철', desc: '3호선 안국역 2번 출구에서 도보 10분' },
      { icon: '🚌', title: '버스', desc: '종로02 마을버스 · 북촌한옥마을 하차' },
      { icon: '🅿️', title: '주차', desc: '전용 주차장이 없어 대중교통 이용을 권장드립니다' },
    ],
  },

  // ---------------------------------------------------------
  // 축의금 계좌 (계좌 포함 버전에서만 노출)
  // ---------------------------------------------------------
  account: {
    // 기본 노출 여부. false = 메인은 계좌 숨김 / ?account=on 링크에서만 계좌 보임
    defaultVisible: false,
    groom: [
      { relation: '신랑', name: '박지용', bank: '국민은행', number: '123456-01-234567', kakaopay: '' },
      { relation: '아버지', name: '박희서', bank: '신한은행', number: '110-123-456789', kakaopay: '' },
      { relation: '어머니', name: '설옥선', bank: '농협은행', number: '302-1234-5678-91', kakaopay: '' },
    ],
    bride: [
      { relation: '신부', name: '오애신', bank: '카카오뱅크', number: '3333-01-2345678', kakaopay: '' },
      // 신부 아버님은 고인이시라 계좌 행을 두지 않았습니다. 필요하면 다시 추가하세요.
      { relation: '어머니', name: '김현', bank: '하나은행', number: '123-456789-01234', kakaopay: '' },
    ],
  },

  // ---------------------------------------------------------
  // 방명록 / 참석여부(RSVP) 데이터 저장 (영속화)
  // ---------------------------------------------------------
  // 아래 supabase 값이 비어 있으면 접속 기기(localStorage)에만 저장됩니다(미리보기용).
  // 하객들과 공유·영구 보존하려면 Supabase 프로젝트를 만들고 아래에 넣으세요.
  //   1) https://supabase.com 에서 프로젝트 생성
  //   2) docs/supabase-setup.md 의 SQL 을 SQL Editor 에 붙여넣고 실행
  //   3) Project URL 과 anon public key 를 아래에 입력
  // anon key 는 원래 공개되는 클라이언트용 키라 커밋해도 안전합니다(보안은 RLS로 제어).
  supabase: {
    url: 'https://pkqdrrsruxjbchzymujd.supabase.co',
    // publishable key (구 anon key). 공개용 클라이언트 키라 커밋 안전.
    anonKey: 'sb_publishable_ISk4ObsaY9D2wa0oawAPOQ_tTRXteh8',
  },
  guestbook: { enabled: true },
  rsvp: {
    enabled: true,
    // 참석여부가 제출되면 메일로도 알려줍니다. endpoint 가 비어 있으면
    // 메일은 보내지 않고 Supabase 에만 기록됩니다(대시보드에서 확인).
    //  · FormSubmit(가입 불필요): 'https://formsubmit.co/ajax/받는주소@example.com'
    //    첫 제출 때 오는 인증 메일을 한 번 클릭해야 이후 메일이 옵니다.
    //  · Web3Forms(가입 필요): endpoint 'https://api.web3forms.com/submit' + accessKey
    // FormSubmit 이 발급한 난수 주소(Invisible Emails). 받는 메일 주소가
    // 코드에 전혀 남지 않으므로 수집 봇이 가져갈 것이 없습니다.
    // 받는 사람을 바꾸려면 FormSubmit 에서 새 주소를 발급받아 교체하세요.
    email: {
      endpoint: 'https://formsubmit.co/ajax/4008ff57bc5d3af1675f7be92a30349b',
      accessKey: '',
    },
    // 첫 화면(인트로)이 끝나고 본문으로 넘어갈 때 뜨는 참석 여부 안내 팝업.
    // 팝업과 본문 "참석 여부" 섹션은 같은 폼을 쓰며, 어느 쪽에서 작성해도 동일하게 저장됩니다.
    popup: {
      enabled: true,
      delay: 900, // 본문 전환 후 이 시간(ms) 뒤에 팝업이 뜹니다.
      eyebrow: '초대',
      title: '참석 여부를 알려주세요',
      message: `저희 예식은 백인제가옥에서
가까운 분들만 모시고 조용히 치러집니다.

공간이 협소한 탓에 준비할 수 있는 자리가
한정되어 있어, 미리 참석 여부를 알려주시면
한 분 한 분 부족함 없이 모실 수 있습니다.

번거로우시겠지만 잠시만 시간을 내어주시면
정말 감사하겠습니다.`,
    },
  },
}

export default config
